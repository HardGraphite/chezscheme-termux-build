Build Script for Chez Scheme on Termux
======================================

- run ``make`` or ``make build`` to download source files and build
- run ``make install`` to install; run ``make uninstall`` to uninstall
